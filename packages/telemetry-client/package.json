{
  "name": "@darwin/telemetry-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage --coverage.thresholds.lines=82 --coverage.thresholds.functions=84 --coverage.thresholds.statements=82 --coverage.thresholds.branches=60"
  },
  "dependencies": {
    "@darwin/shared": "0.1.0"
  },
  "devDependencies": {
    "jsdom": "^30.0.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
